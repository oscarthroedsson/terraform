# Ignore Terraform state files
terraform.tfstate
terraform.tfstate.backup

# Ignore Terraform provider plugins
.terraform/

# Ignore Terraform lock file (optional if you don't want to share provider versions)
.terraform.lock.hcl

# Ignore sensitive JSON files (e.g., API keys)
*.json
terraform-key.json
